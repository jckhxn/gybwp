import config from '../config';

// ...existing code...

console.log('YouTube Channel ID:', config.youtubeChannelId);
console.log('YouTube API Key:', config.youtubeApiKey);
console.log('Sanity Project ID:', config.sanityProjectId);
console.log('Sanity Dataset:', config.sanityDataset);
console.log('Sanity Read Token:', config.sanityReadToken);

// ...existing code...
